organization := "$organization$"

name := "$name$"

version := "$version$"

scalaVersion := "$scala_version$"

libraryDependencies ++= Seq(
    // add dependencies here
    // eg. "org.scalatest" %% "scalatest" % "2.0.M5b" % "test"
)
